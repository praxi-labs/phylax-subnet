# Core
# bittensor pinned because validator and miner must agree on the synapse wire
# format. v9 and v10 dendrite encodings are not compatible — mismatched
# versions surface as SynapseParsingError on the receiving axon.
bittensor==10.4.0
pydantic
torch
numpy

# Static analysis (optional but recommended)
bandit

# SBOM
cyclonedx-bom

# Cryptography / SS58
substrate-interface
PyNaCl

# Networking / HTTP
requests
httpx

# SKILL.md capability manifest parsing (YAML frontmatter)
pyyaml

# REST API surface (validator side)
fastapi
uvicorn[standard]

# Attestation registry persistence
sqlalchemy

# TOML parsing on py3.10 (3.11+ has tomllib built in)
tomli ; python_version < "3.11"

# Testing
pytest
pytest-asyncio
