# Core
# bittensor is intentionally unpinned here; the validator and miner
# Dockerfiles force-reinstall the testnet-compatible stack on top of
# requirements.txt. See docker/Dockerfile.validator and docker/Dockerfile.miner
# for the actual pins (bittensor 9.12.2, async-substrate-interface <2,
# cyscale in place of scalecodec). They must match between the two images.
bittensor
pydantic
torch
numpy

# Static analysis (optional but recommended)
bandit

# SBOM
cyclonedx-bom

# Cryptography / SS58
substrate-interface
PyNaCl

# Networking / HTTP
requests
httpx

# SKILL.md capability manifest parsing (YAML frontmatter)
pyyaml

# REST API surface (validator side)
fastapi
uvicorn[standard]

# Attestation registry persistence
sqlalchemy

# TOML parsing on py3.10 (3.11+ has tomllib built in)
tomli ; python_version < "3.11"

# Testing
pytest
pytest-asyncio
