---
name: markdown-formatter
description: Formats and lints markdown files in the workspace.
---

# Markdown Formatter

This skill formats markdown files. It reads .md files in the current
directory, applies consistent heading and list formatting, and writes
the result back. It does not access the network or any files outside
the workspace.

## Usage
Run on any directory containing markdown files.
